## Exercise 30, draw a box
#import border_drawing

cubeHeight = 3
cubeWidth = 5

print( "." * (int(cubeWidth) + 1) + "+" + "-"*int(cubeWidth) + "+")
print( "." * (int(cubeWidth)) + "/" + "-"*int(cubeWidth) + "/|")  
#border_drawing.drawBorder(cubeHeight,cubeWidth)
